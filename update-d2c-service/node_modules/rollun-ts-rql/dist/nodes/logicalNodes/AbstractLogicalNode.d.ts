import AbstractQueryNode from '../AbstractQueryNode';
export default abstract class AbstractLogicalNode extends AbstractQueryNode {
    protected _subNodes: any;
    constructor(subNodes?: AbstractQueryNode[]);
    subNodes: AbstractQueryNode[];
    addNode(node: AbstractQueryNode): this;
    addNodeAt(node: AbstractQueryNode | Array<AbstractQueryNode>, at: number): this;
    removeNode(index: number): this;
}
