import { NodeParserInterface } from './interfaces';
import TokenStream from './TokenStream';
export default class NodeParserChain implements NodeParserInterface {
    protected nodeParsers: NodeParserInterface[];
    addNodeParser(nodeParser: NodeParserInterface): this;
    parse(tokenStream: TokenStream): import("../nodes/AbstractNode").default;
    supports(tokenStream: TokenStream): boolean;
}
