import { SubParserInterface } from '../interfaces';
import TokenStream from '../TokenStream';
export default class FieldParser implements SubParserInterface {
    parse(tokenStream: TokenStream): string;
}
