import { SubParserInterface } from '../interfaces';
import TokenStream from '../TokenStream';
export default class IntegerParser implements SubParserInterface {
    parse(tokenStream: TokenStream): number;
}
