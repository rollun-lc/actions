import Token, { TokenTypeNameMap } from './Token';
export default class TokenStream {
    private tokens;
    private current;
    constructor(tokens: Token[]);
    count(): number;
    readonly length: number;
    next(): Token;
    nextIf(type: TokenTypeNameMap, value?: string | string[]): Token | null;
    test(type: TokenTypeNameMap | TokenTypeNameMap[], value?: string | string[]): boolean;
    lookAhead(tokenNumber?: number): Token;
    expect(type: TokenTypeNameMap | TokenTypeNameMap[], value?: string | string[]): Token;
    getCurrent(): Token;
    isEnd(): boolean;
}
