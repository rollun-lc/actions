import { SubLexerInterface } from '../interfaces';
import Token from '../Token';
export default class RqlOperatorSubLexer implements SubLexerInterface {
    getTokenAt(code: any, cursor: any): Token;
}
