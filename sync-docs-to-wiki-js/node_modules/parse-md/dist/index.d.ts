declare type Content = string;
declare type EmptyObject = Record<string, never>;
declare type InputContent = string;
declare type Line = string;
declare type MetaIndex = number;
interface ParseMeta {
    ({ lines, metaIndices }: {
        lines: Line[];
        metaIndices: MetaIndex[];
    }): EmptyObject | unknown;
}
declare const parseMeta: ParseMeta;
interface ParseContent {
    ({ lines, metaIndices }: {
        lines: Line[];
        metaIndices: MetaIndex[];
    }): Content;
}
declare const parseContent: ParseContent;
export interface ParseMD {
    (contents: InputContent): {
        metadata: ReturnType<typeof parseMeta>;
        content: ReturnType<typeof parseContent>;
    };
}
declare const parseMD: ParseMD;
export default parseMD;
