declare type RefreshNodeRedModuleParams = {
    name: string;
    baseUrl: string;
};
export declare const refreshNodeRedModule: ({ name, baseUrl, }: RefreshNodeRedModuleParams) => Promise<void>;
export {};
